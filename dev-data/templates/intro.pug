doctype html
html 
    head 
        //-also has interpolation
        title Natours #{tour} 
        link(rel='stylesheet' href='css/style.css') 
        link(rel='shortcut icon' type='image/png' href='img/favicon.png')
    body 
        h1= tour
        // this is a comment in html that will appear in the html file
        //- this is a pug file comment 
        //-this is buffered code  and simple javascript can be written here if you start with
        p= user.toUpperCase() 
        //-unbuuffered code doesnt add anything to the html file or the output and it starts with a hyphen
        - const x = 2
        h2= 2 * x

